# Use a dedicated database, created separately in PostgreSQL/pgAdmin.
# Never use the clinical database or production credentials here.
TEST_DATABASE_URL="postgresql://test_user:replace-me@localhost:5432/orthoflow_test?schema=public"
